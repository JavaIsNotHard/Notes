tells kubectl which cluster to talk to, which credentials to use, and which namespace to default to

**Context** = Choosing which office building to go to and which ID badge to wear
**Node Authorizer** = The security guard inside the building checking if your badge allows access to specific rooms

