ensures that a copy of a pod runs on all nodes in your cluster
when new nodes are added then that pod is automatically added to them
when nodes are removed, these pods are garbage collected

logging and monitoring agents that extract information from each node can be of type DaemonSet

 

