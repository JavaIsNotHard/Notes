
basically we can trigger EC2 ASG based on the message that are buffered in our SQS
if 1000s of message in the SQS queue but consumption rate is slow then it can trigger ASG through cloudwatch metrics


SQS as a buffer to database writes