managed database service that gives you both AWS automation (backups, monitoring, replication) and access to the underlying OS and DB engine

This allows us to SSH into our RDS managed database since the database is running inside an EC2 instance