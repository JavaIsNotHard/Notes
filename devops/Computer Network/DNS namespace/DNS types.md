[[recursive dns]]
[[iterative dns]]

we can have a combination of both [[recursive dns]] and [[iterative dns]] in an dns implementation where the interaction between the client and the local resolver or remote dns resolver is recursive and the interaction between the resolver and other servers are iterative