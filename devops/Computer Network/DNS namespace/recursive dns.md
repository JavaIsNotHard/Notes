type of dns where servers don't immediately response but rather further sends request on behalf o the requesting client, be it the actual client or the root dns or the TLD servers, here the requesting client is the one sending DNS request to another server for answers

