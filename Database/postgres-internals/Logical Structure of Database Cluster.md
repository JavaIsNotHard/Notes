A "database cluster" is simply **all the databases managed by one PostgreSQL server instance** on a single machine. For example, one PostgreSQL server might manage databases called `users_db`, `orders_db`, and `analytics_db` — together, those form a "cluster."


